-- INCLUSIÓN DE LOS CAMPOS IVA, SUBTOTAL IVA, Y COSTO TOTAL EN LA CABECERA DE FACTURAS
ALTER TABLE `tab_fac_cab_facturas` ADD `SUBT_IVA_CAB_FACT` DECIMAL(10,2) NOT NULL DEFAULT '0' , ADD `IVA_CAB_FACT` DECIMAL(10,2) NOT NULL DEFAULT '0' AFTER `SUBT_IVA_CAB_FACT`, ADD `COSTO_TOT_CAB_FACT` DECIMAL(10,2) NOT NULL DEFAULT '0' AFTER `IVA_CAB_FACT`;

ALTER TABLE `tab_fac_cab_facturas` ADD `ESTADO_IMP_FAC` CHAR(1) NOT NULL DEFAULT 'N' AFTER `COD_CAB_FACT`;

-- MODIFICACIONES PARA TERCER SPRINT
ALTER TABLE `tab_fac_clientes` CHANGE `E_MAIL_CLI` `E_MAIL_CLI` VARCHAR(50) CHARACTER SET latin1 COLLATE latin1_swedish_ci NOT NULL;

ALTER TABLE `tab_fac_clientes` CHANGE `FECHA_NAC_CLI` `FECHA_NAC_CLI` DATE NULL DEFAULT NULL;

ALTER TABLE `tab_fac_clientes` CHANGE `DIRECCION_CLI` `DIRECCION_CLI` VARCHAR(100) CHARACTER SET latin1 COLLATE latin1_swedish_ci NULL DEFAULT NULL;

ALTER TABLE `tab_fac_usuarios` CHANGE `FECHA_NAC_USU` `FECHA_NAC_USU` DATE NULL DEFAULT NULL;

ALTER TABLE `tab_fac_usuarios` CHANGE `DIRECCION_USU` `DIRECCION_USU` VARCHAR(100) CHARACTER SET latin1 COLLATE latin1_swedish_ci NULL DEFAULT NULL;

ALTER TABLE `tab_fac_usuarios` CHANGE `FONO_USU` `FONO_USU` VARCHAR(10) CHARACTER SET latin1 COLLATE latin1_swedish_ci NULL DEFAULT NULL;

